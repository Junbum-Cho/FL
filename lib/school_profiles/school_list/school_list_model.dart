import '/components/each_school_list_widget.dart';
import '/flutter_flow/flutter_flow_util.dart';
import 'school_list_widget.dart' show SchoolListWidget;
import 'package:flutter/material.dart';

class SchoolListModel extends FlutterFlowModel<SchoolListWidget> {
  ///  State fields for stateful widgets in this page.

  // Model for eachSchoolList component.
  late EachSchoolListModel eachSchoolListModel1;
  // Model for eachSchoolList component.
  late EachSchoolListModel eachSchoolListModel2;
  // Model for eachSchoolList component.
  late EachSchoolListModel eachSchoolListModel3;
  // Model for eachSchoolList component.
  late EachSchoolListModel eachSchoolListModel4;
  // Model for eachSchoolList component.
  late EachSchoolListModel eachSchoolListModel5;
  // Model for eachSchoolList component.
  late EachSchoolListModel eachSchoolListModel6;
  // Model for eachSchoolList component.
  late EachSchoolListModel eachSchoolListModel7;
  // Model for eachSchoolList component.
  late EachSchoolListModel eachSchoolListModel8;
  // Model for eachSchoolList component.
  late EachSchoolListModel eachSchoolListModel9;
  // Model for eachSchoolList component.
  late EachSchoolListModel eachSchoolListModel10;
  // Model for eachSchoolList component.
  late EachSchoolListModel eachSchoolListModel11;

  @override
  void initState(BuildContext context) {
    eachSchoolListModel1 = createModel(context, () => EachSchoolListModel());
    eachSchoolListModel2 = createModel(context, () => EachSchoolListModel());
    eachSchoolListModel3 = createModel(context, () => EachSchoolListModel());
    eachSchoolListModel4 = createModel(context, () => EachSchoolListModel());
    eachSchoolListModel5 = createModel(context, () => EachSchoolListModel());
    eachSchoolListModel6 = createModel(context, () => EachSchoolListModel());
    eachSchoolListModel7 = createModel(context, () => EachSchoolListModel());
    eachSchoolListModel8 = createModel(context, () => EachSchoolListModel());
    eachSchoolListModel9 = createModel(context, () => EachSchoolListModel());
    eachSchoolListModel10 = createModel(context, () => EachSchoolListModel());
    eachSchoolListModel11 = createModel(context, () => EachSchoolListModel());
  }

  @override
  void dispose() {
    eachSchoolListModel1.dispose();
    eachSchoolListModel2.dispose();
    eachSchoolListModel3.dispose();
    eachSchoolListModel4.dispose();
    eachSchoolListModel5.dispose();
    eachSchoolListModel6.dispose();
    eachSchoolListModel7.dispose();
    eachSchoolListModel8.dispose();
    eachSchoolListModel9.dispose();
    eachSchoolListModel10.dispose();
    eachSchoolListModel11.dispose();
  }
}
